<h2>Create new rating</h2>

<%= form_for(@rating) do |f| %>
  <%= f.select :coffee_id, options_from_collection_for_select(@coffees, :id, :to_s) %>
  score: <%= f.number_field :score %>

  <%= f.submit %>
<% end %>

<%= link_to 'Back', ratings_path %>
