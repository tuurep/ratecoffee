<%= form_for(@coffee) do |f| %>
  <% if @coffee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coffee.errors.count, "error") %> prohibited this coffee from being saved:</h2>

      <ul>
      <% @coffee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :style %><br>
    <%= f.text_field :style %>
  </div>
  <div class="field">
    <%= f.label :roastery_id %><br>
    <%= f.number_field :roastery_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
