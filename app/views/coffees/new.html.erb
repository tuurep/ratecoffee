<h1>New Coffee</h1>

<%= form_for(@coffee) do |f| %>
  <%= f.select :roastery_id, options_from_collection_for_select(@roasteries, :id, :name) %>
  name: <%= f.text_field :name %>
  <%= f.select :style, options_for_select(@styles) %>

  <%= f.submit %>
<% end %>

<%= link_to 'Back', coffees_path %>
