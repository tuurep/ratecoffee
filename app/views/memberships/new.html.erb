<h1>New Membership</h1>

<%= form_for(@membership) do |f| %>
  <%= f.select :coffee_club_id, options_from_collection_for_select(@coffee_clubs, :id, :name) %>
  <%= f.submit %>
<% end %>

<%= link_to 'Back', memberships_path %>
