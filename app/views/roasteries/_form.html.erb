<%= form_for(@roastery) do |f| %>
  <% if @roastery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@roastery.errors.count, "error") %> prohibited this roastery from being saved:</h2>

      <ul>
      <% @roastery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :year %><br>
    <%= f.number_field :year %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
